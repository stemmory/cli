---
feature: legacy/aliased-feature
title: Legacy Aliased Feature
---

## Why
Written before schema v1 introduced `slug:`. Still parses successfully using
the legacy `feature:` key (decision D-1 #1), with a warning pointing the
author at `slug:`.
