---
title: No Slug Or Feature Key
type: feature
---

Neither `slug:` nor the legacy `feature:` is present, so this document cannot
become a node at all. CONVENTIONS.md §2: a silent skip, not a sync error.
