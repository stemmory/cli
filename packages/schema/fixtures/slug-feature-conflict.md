---
slug: auth/social-login
feature: billing/invoices
title: Conflicting Keys
---

## Why
`slug:` and `feature:` name two different documents — this cannot mean
anything, so it is an error (decision D-1 #1), not a warning.
