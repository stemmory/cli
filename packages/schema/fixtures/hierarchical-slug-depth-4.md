---
slug: auth/roles/admin/permissions
title: Admin role permissions
updated: 2026-08-01
---

## Why
CONVENTIONS.md §3: slugs are the full `/`-separated hierarchical path, max
depth 4 — not the flat, filename-matching form §2.4's own example comment
suggests. 9 of the 16 existing feature docs use multi-segment slugs
(`auth/roles`, `billing/plan-changes`, `sync/github-docs`, ...); this fixture
exercises the deepest depth the grammar allows.
